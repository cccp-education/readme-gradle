= PROMPT_REPRISE — Chiraq (readme-plugin) — Session 013
:date: 2026-09-07
:session: 013

== Session 012 terminée — US-ROLLOUT-6 adoption version catalog (cross-borough MEMPHIS)

EPIC MEM-CAT-ROLLOUT ordre 6 (référentiel `workspace-bom/BACKLOG.adoc`).

Pin `ws` 0.0.31 dans settings (D4) + `version = ws.versions.readme.plugin.get()`
(D3, remplace dérivation toml local) + id inline `education.cccp.readme`
(stable publication identity). Garde `ReadmePluginPublicationTest` 3/3
(RED → GREEN, mirror `CodexPluginPublicationTest` S-211).
Gate : `check` VERT 83 tests 0 FAIL (22 unit + 5 FT + 56 cucumber, baseline
80 → +3) + `publishToMavenLocal` VERT (POM 0.0.1 version dérivée ws).
Rapport : `.agents/sessions/012-us-rollout-6-version-catalog.adoc`.

== Prochaine session — 013 : RM-12-2 refonte README article 0124

- README.md racine (consommateur) : perd sections dev (build/test/
  dépendances), gagne Clone & Use + Personalization (leviers readme.yml).
- readme-plugin/README.md (plugin) : gagne Périmètre (fait/ne fait pas)
  + Tâches Gradle exhaustives (inputs/outputs/dépendances), perd Quick Start.
- README_truth.adoc audience = plugin confirmée (référence canonique).
- Sync 20 fichiers i18n (10 langues × 2 audiences) — manuel pour cette
  US, l'automatisation arrive en RM-12-5.

Puis enchaîner :
- RM-12-3 : Infra LLM `readme.llm` (port + adapter + LlmBuildService N1)
- RM-12-4 : Domaine `readme.i18n` + adapter (pattern bakery)
- RM-12-5 : Tâche `translateReadme` + dogfooding 20 fichiers
- RM-12-6 : Cucumber BDD `readme_i18n.feature`

== Contexte

Borough CHIRAQ 🟤 — 13ème borough. Publié Maven Central (0.0.1).
Version désormais dérivée du catalog ws (MEM-CAT-ROLLOUT-6 ✅ S-012).
EPIC RM-12 : US-0 ✅ S-010, US-1 ✅ S-011, US-2→6 À FAIRE.
Dette technique : RM-1/5/6/8/9/10 À FAIRE (post RM-12-2).

== Rappel

- Git autorisé (décision pilote 2026-09-17) — zéro secret commité
- Vérifier AGENT.adoc avant d'agir
- Pas de tests en fin de session sans permission
- Placeholder `<YOUR_GITHUB_PAT>` = sentinel officiel (à préserver)
- Pattern post-SLD-8 : consommer `LlmBuildService` de codebase N1,
  ne pas dupliquer `LlmProviderChatModelAdapter` (consolidé N1 S-073)