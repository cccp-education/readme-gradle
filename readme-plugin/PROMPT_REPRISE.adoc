= PROMPT_REPRISE — Chiraq (readme-plugin) — Session 011
:date: 2026-08-24
:session: 011

== Session 010 terminée — Cadrage EPIC RM-12 (readme-LLM) + RM-4b (auto-traduction i18n)

Cadrage EPIC RM-12 + RM-4b (gouvernance pure, zéro code, pattern S-057/S-063).

Audit 8 constats :
1. readme-gradle n'utilise aucun LLM ni contexte augmenté aujourd'hui
2. `alias(libs.plugins.codebase)` appliqué mais codebase n'est pas consommé
3. Bundle `readme.ai` compileOnly est du code mort (6 modules langchain4j)
4. `node-gradle` (RM-7) est aussi du code mort
5. README consommateur et README plugin se chevauchent
6. 20 fichiers i18n générés manuellement (session 007), pas de régénération auto
7. Bakery a un pattern i18n mature (EPIC BKY-I18N terminé)
8. Slider a le pattern post-SLD-8 pour la consommation codebase

11 décisions architecturales :
- Pattern post-SLD-8 (consommation `LlmBuildService` de codebase N1)
- Domaine `readme.llm` (1er) + `readme.i18n` (2ème)
- Tâche `translateReadme` additive (3 tâches existantes inchangées)
- Cleanup RM-7 étendu (node-gradle + readme.ai bundle)
- Refonte README selon article 0124 (RM-11 absorbé)
- RM-2 + RM-4b absorbés dans RM-12
- 7 US ~17 pts

Doc `CADRAGE_EPIC_RM_I18N.adoc` créé.

== Prochaine session — 011 : RM-12-1 cleanup dépendances

- RM-12-1 : Suppression `node-gradle` (`build.gradle.kts:47`) + suppression
  bundle `readme.ai` compileOnly (`build.gradle.kts:43`, 6 modules langchain4j
  caducs). `compileKotlin` VERT. ~3 unit (non-régression compile).

Puis enchaîner :
- RM-12-2 : Refonte README consommateur vs plugin selon article 0124
- RM-12-3 : Infra LLM `readme.llm` (port + adapter + LlmBuildService N1)
- RM-12-4 : Domaine `readme.i18n` + adapter (pattern bakery)
- RM-12-5 : Tâche `translateReadme` + dogfooding 20 fichiers
- RM-12-6 : Cucumber BDD `readme_i18n.feature`

== Contexte

Borough CHIRAQ 🟤 — 13ème borough. Publié Maven Central (0.0.1).
EPIC RM-12 cadré (7 US ~17 pts). US-0 ✅, US-1→6 À FAIRE.
Dette technique : RM-1/5/6/8/9/10 À FAIRE (post RM-12-2).

== Rappel

- INTERDICTION de commit/push sans permission
- Vérifier AGENT.adoc avant d'agir
- Pas de tests en fin de session sans permission
- Placeholder `<YOUR_GITHUB_PAT>` = sentinel officiel (à préserver)
- Pattern post-SLD-8 : consommer `LlmBuildService` de codebase N1,
  ne pas dupliquer `LlmProviderChatModelAdapter` (consolidé N1 S-073)